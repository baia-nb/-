# 2026-08-15 工作日志

## 新建「网易版↔国际版 我的世界存档转换器」（路线2 全套）
- 用户贴了一份完整方案文档（前端+后端+数据库、零月租），要求按**路线2（Node 后端 + Supabase + Cloudflare Pages）+ 双向（解密+加密）**落地，并先恢复 baianb.kdns.fr 隧道。
- 恢复隧道：本地 :8000 后端 + cloudflared 均曾掉线（530）。已用托管 Python 重启 uvicorn（PID 另起）+ scripts/cloudflared.exe + 令牌重连，公网 baianb.kdns.fr 恢复 200。
- 算法核对（GitHub 现已可达，已 clone 验证）：
  - `ihaiming/NetEaseMC-Decryptor`（index.html 内 getKey/decryptFile）与 `HTMonkeyG/XOREncryptHelper`。
  - 加密文件以魔数 `80 1D 30 01` 开头；密钥 K 由该世界 `CURRENT` 密文 与 `MANIFEST` 文件名(UTF-8+0x0A) 做 XOR 推出（CURRENT 明文恰为 manifest 文件名串，故 K 可稳定还原）；解密 `明文=密文[4:] XOR K`。加解密同一套 XOR，可逆。
- 新建独立项目 `netease-converter/`（与 baianb 现有 Python 应用互不干扰）：
  - `backend/decrypt/netease.js`：核心加解密 + zip 处理（解密=逐字节核对开源；加密=XOR 逆向，用 CURRENT 明文长度构造 K 保证双向可逆）。
  - `backend/src/{api,task,db,cleanup}.js`：Express API（/api/upload 入队、/api/task/:id 轮询、/api/download/:id、/api/feedback）、顺序任务队列、IP 限流（每IP每小时20次）、临时文件 2h 自动清理；Supabase 可选（未配置降级本地 JSON）。
  - `frontend/`：纯静态（index.html/app.js/style.css/config.js），拖拽上传、进度、轮询、下载、双向模式切换、FAQ+法律边界；可直部署 Cloudflare Pages。
  - `supabase/schema.sql`（records/feedback）、`backend/render.yaml`、`.env.example`、`README.md`、`scripts/{make_sample,e2e}.js`。
- 验证：
  - `npm test`（算法往返）全过。
  - e2e（HTTP 全链路：加密→下载→解密→下载）内部文件逐字节一致，双向可逆。
- **待用户自行完成（需其账号，我无法代劳）**：把 backend/ 推 GitHub→Render 建 Web Service；frontend/ 推 GitHub→Cloudflare Pages（改 config.js 的 API_BASE 为 Render 地址）；可选 Supabase 建表+填环境变量。域名/SSL 走 Cloudflare 免费层。
- 注意：本方案仅做玩家自有存档格式转换，不含登录协议/联机绕过；资源工坊加密地图无法解密（属已知限制）。
